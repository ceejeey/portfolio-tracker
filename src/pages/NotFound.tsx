import { Link } from 'react-router-dom';

const NotFound = ({}) => {
    return (
        <>
            <Link to="/">Go Home</Link>

            <div>NotFound</div>
        </>
    );
};

export default NotFound;
